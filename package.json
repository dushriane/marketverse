{
  "name": "marketverse",
  "private": true,
  "version": "0.1.0",
  "description": "MarketVerse monorepo (apps + shared packages)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo \"No builds defined yet\"",
    "test": "echo \"No tests yet\""
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10"
}
